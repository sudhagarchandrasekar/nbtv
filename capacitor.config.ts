import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uk.nbtv.v2',
  appName: 'nakkubettaTV',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
